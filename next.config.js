/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['localhost','meta-softic-blog-management.onrender.com'],
    },
};

module.exports = nextConfig;
